CMAKE_MINIMUM_REQUIRED(VERSION 3.0.0)
PROJECT(ankh VERSION 1.0.0)

SET(TEST_EXE ankhtests)
SET(TEST_LIB catch)

ADD_LIBRARY(${TEST_LIB} STATIC catch.cpp)

ADD_EXECUTABLE(${TEST_EXE} 
    tests.cc
    lexer_tests.cc
    parser_tests.cc
    interpreter_tests.cc
)

TARGET_INCLUDE_DIRECTORIES(${TEST_EXE} PRIVATE ${CMAKE_SOURCE_DIR}/include)
TARGET_INCLUDE_DIRECTORIES(${TEST_LIB} PRIVATE ${CMAKE_SOURCE_DIR}/include)
TARGET_LINK_LIBRARIES(${TEST_EXE} ${TEST_LIB} ${APP_LIB} ${FMT_LIB})
